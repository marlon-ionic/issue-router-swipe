import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'simple-router-swipe',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
